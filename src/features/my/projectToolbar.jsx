const ProjectToolbar = () => {
    return (
        <div className="project-toolbar">
            <button>Filter ▼</button>
            <button>Today ▼</button>
        </div>
    );
};

export default ProjectToolbar;
